

{
  "entities": {
    "Team": {
      "title": "Team",
      "type": "object",
      "description": "Represents a team, division, or department within the organization.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Team."
        },
        "name": {
          "type": "string",
          "description": "The name of the team (e.g., 'Parking Facilities')."
        },
        "department": {
          "type": "string",
          "description": "The name of the parent department (e.g., 'Transportation and Mobility')."
        }
      },
      "required": ["id", "name", "department"]
    },
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a system user.",
      "properties": {
        "uid": {
          "type": "string",
          "description": "Unique identifier for the User entity, from Firebase Auth."
        },
        "name": {
          "type": "string",
          "description": "The name of the user."
        },
        "email": {
          "type": "string",
          "description": "The email address of the user.",
          "format": "email"
        },
        "role": {
          "type": "string",
          "enum": ["Admin", "Staff", "Viewer"],
          "description": "The role of the user within their assigned team."
        },
        "teamId": {
          "type": "string",
          "description": "The ID of the user's currently active team."
        },
        "teamIds": {
            "type": "array",
            "description": "A list of team IDs the user has access to.",
            "items": {
                "type": "string"
            }
        }
      },
      "required": [
        "uid",
        "name",
        "email",
        "role",
        "teamId",
        "teamIds"
      ]
    },
    "Task": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Task",
      "type": "object",
      "description": "Represents a maintenance or facility task for a specific team.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Task."
        },
        "categoryId": {
          "type": "string",
          "description": "Reference to a Category within the team."
        },
        "description": {
          "type": "string",
          "description": "Detailed explanation of the task."
        },
        "locationId": {
          "type": "string",
          "description": "Reference to a Location within the team."
        },
        "requestedCompletionDate": {
          "type": "string",
          "format": "date",
          "description": "The date by which the task completion is requested."
        },
        "submitToReviewDate": {
          "type": "string",
          "format": "date",
          "description": "The date the task was submitted for review (staff completion date)."
        },
        "actualCompletionDate": {
          "type": "string",
          "format": "date",
          "description": "The date the task was approved and officially completed."
        },
        "status": {
          "type": "string",
          "description": "The current status of the task."
        },
        "photos": {
          "type": "array",
          "description": "A list of photos associated with the task.",
          "items": {
            "type": "object",
            "properties": {
              "url": {
                "type": "string",
                "description": "Public URL of the photo."
              },
              "path": {
                "type": "string",
                "description": "Storage path of the photo."
              },
              "createdAt": {
                "type": "string",
                "format": "date-time",
                "description": "The date the photo was uploaded."
              }
            },
            "required": ["url", "path", "createdAt"]
          }
        },
        "approvedBy": {
          "type": "string",
          "description": "The UID of the user who approved the task completion."
        },
        "assignedToIds": {
            "type": "array",
            "description": "A list of user IDs assigned to the task.",
            "items": {
                "type": "string"
            }
        }
      },
      "required": [
        "id",
        "categoryId",
        "description",
        "locationId",
        "requestedCompletionDate",
        "status"
      ]
    },
    "RecurringTask": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "RecurringTask",
      "type": "object",
      "description": "Represents a recurring task schedule for a team.",
      "properties": {
        "title": {
          "type": "string",
          "description": "The name of the recurring task."
        },
        "categoryId": {
          "type": "string",
          "description": "Reference to a Category."
        },
        "locationId": {
          "type": "string",
          "description": "Reference to a Location."
        },
        "frequency": {
          "type": "string",
          "enum": [
            "Daily",
            "Weekly",
            "Monthly"
          ],
          "description": "The frequency of the task."
        },
        "dayOfWeek": {
          "type": "number",
          "description": "The day of the week for weekly tasks."
        },
        "weekOfMonth": {
          "type": "number",
          "description": "The week of the month for monthly tasks."
        },
        "lastCompleted": {
          "type": "array",
          "description": "A log of all completion dates for this task.",
          "items": {
            "type": "string",
            "format": "date-time"
          }
        }
      },
      "required": [
        "title",
        "categoryId",
        "locationId",
        "frequency"
      ]
    },
    "Location": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Location",
      "type": "object",
      "description": "Represents a facility location for a team.",
      "properties": {
        "locationName": {
          "type": "string",
          "description": "The name of the location."
        },
        "numberOfFloors": {
          "type": "number",
          "description": "Number of floors at the location. Can be 0 or N/A."
        }
      },
      "required": [
        "locationName"
      ]
    },
    "Category": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Category",
      "type": "object",
      "description": "Represents a task category for a team, which contains sub-categories.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Category."
        },
        "name": {
          "type": "string",
          "description": "The name of the category."
        },
        "color": {
          "type": "string",
          "description": "The color associated with the category."
        },
        "subcategories": {
            "type": "array",
            "description": "A list of sub-categories.",
            "items": {
                "type": "object",
                "properties": {
                    "id": { "type": "string" },
                    "name": { "type": "string" }
                },
                "required": ["id", "name"]
            }
        }
      },
      "required": [
        "id",
        "name"
      ]
    },
    "Setting": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Setting",
      "type": "object",
      "description": "Represents system settings for a team.",
      "properties": {
        "completionDateRange": {
          "type": "number",
          "description": "The minimum number of days for task completion requests."
        },
        "recurringTaskCompletionDays": {
          "type": "number",
          "description": "Days in advance a weekly or monthly recurring task can be completed."
        }
      },
      "required": [
        "completionDateRange"
      ]
    }
  },
  "auth": {
    "providers": [
      "password"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/teams/{teamId}",
        "definition": {
          "entityName": "Team",
          "schema": {
            "$ref": "#/backend/entities/Team"
          },
          "description": "Stores all teams/departments."
        }
      },
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores user profiles, independent of teams."
        }
      },
      {
        "path": "/teams/{teamId}/tasks/{taskId}",
        "definition": {
          "entityName": "Task",
          "schema": {
            "$ref": "#/backend/entities/Task"
          },
          "description": "Stores all tasks for a specific team."
        }
      },
      {
        "path": "/teams/{teamId}/recurringTasks/{recurringTaskId}",
        "definition": {
          "entityName": "RecurringTask",
          "schema": {
            "$ref": "#/backend/entities/RecurringTask"
          },
          "description": "Stores all recurring tasks for a specific team."
        }
      },
      {
        "path": "/teams/{teamId}/locations/{locationId}",
        "definition": {
          "entityName": "Location",
          "schema": {
            "$ref": "#/backend/entities/Location"
          },
          "description": "Stores all facility locations for a specific team."
        }
      },
      {
        "path": "/teams/{teamId}/categories/{categoryId}",
        "definition": {
          "entityName": "Category",
          "schema": {
            "$ref": "#/backend/entities/Category"
          },
          "description": "Stores all task categories for a specific team."
        }
      },
      {
        "path": "/teams/{teamId}/settings/appSettings",
        "definition": {
          "entityName": "Setting",
          "schema": {
            "$ref": "#/backend/entities/Setting"
          },
          "description": "Stores team-specific settings in a single document."
        }
      }
    ]
  }
}

    