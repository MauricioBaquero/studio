rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {

    // Apply rules to all files in the bucket
    match /{allPaths=**} {
      // WARNING: This is insecure and not recommended for production.
      // This allows anyone to read and write to your storage bucket.
      allow read, write;
    }
  }
}
